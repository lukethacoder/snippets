# Salesforce: Reference Static Resource Dynamically

Reference a static resource without hardcoding the `orgId` in the URL. Handy for referencing in `.css` files.

```txt copy
/sfsites/c/resource/
```
