# Make All Page Content Editable

Make all elements on the page editable via the <a href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/contentEditable" target="_blank">`contentEditable`</a> property.

```js copy
document.querySelectorAll('*').forEach(el => el.contentEditable = true)
```